<h2>New content</h2>

<%= render "form.html", changeset: @changeset,
                        types: @types,
                        action: admin_content_path(@conn, :create) %>

<%= link "Back", to: admin_content_path(@conn, :index) %>
